{
  "name": "floraguard-plant-disease-platform",
  "version": "2.4.0",
  "private": true,
  "description": "Enterprise Plant Pathology & Leaf Disease Classification Portal",
  "scripts": {
    "dev": "npm --prefix frontend run dev",
    "build": "npm --prefix frontend run build",
    "preview": "npm --prefix frontend run preview",
    "test": "npm --prefix frontend run test",
    "lint": "npm --prefix frontend run lint"
  },
  "devDependencies": {
    "typescript": "^5.4.2"
  }
}
